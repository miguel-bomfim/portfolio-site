import CircularProgress from "@mui/material/CircularProgress";
import "./Loading.css";

const Loading = () => {
  return <CircularProgress size={50} className="loadingComponent" />;
};

export default Loading;
